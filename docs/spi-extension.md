# SPI Extension

Implement `com.document.parsing.core.parser.DocumentParser` and register it in:

`META-INF/services/com.document.parsing.core.parser.DocumentParser`

Example entry:

```
com.example.CustomDocumentParser
```
